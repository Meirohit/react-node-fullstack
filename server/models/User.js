export class User {
    constructor(id, email, full_name, created_at) {
        this.id = id;
        this.email = email;
        this.full_name = full_name;
        this.created_at = created_at;
    }
}